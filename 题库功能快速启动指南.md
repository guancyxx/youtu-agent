# 题库功能快速启动指南

## 🚀 启动步骤

### 1. 启动 QDUOJ 后端
确保 QDUOJ 在线判题系统运行在 `http://localhost:8000`

### 2. 配置环境变量（可选）
在 `youtu-agent/frontend/webui/` 目录创建 `.env` 文件：
```bash
VITE_QDUOJ_API_URL=http://localhost:8000
```

### 3. 安装依赖（首次运行）
```bash
cd youtu-agent/frontend/webui
npm install
```

### 4. 启动前端
```bash
npm run dev
```

### 5. 打开浏览器
访问显示的本地地址（通常是 `http://localhost:5173`）

## 🎯 使用方法

1. **打开题库**：点击右上角的书本图标 📚
2. **浏览题目**：查看题目列表、难度、标签、通过率
3. **搜索题目**：在搜索框输入关键词
4. **筛选题目**：
   - 点击难度按钮（简单/中等/困难）
   - 勾选标签复选框
5. **选择题目**：点击题目卡片，自动填充到聊天输入框
6. **与AI对话**：发送消息，获取编程指导

## 🔧 常见问题

### Q: 题库按钮不显示？
**A**: 检查浏览器控制台是否有错误，确认所有文件已保存并重新加载页面。

### Q: 点击题库按钮无反应？
**A**: 
1. 检查 QDUOJ 后端是否运行
2. 打开浏览器开发者工具查看 Network 请求
3. 确认 API 地址配置正确

### Q: 题目列表为空？
**A**: 
1. 确认 QDUOJ 数据库中有题目数据
2. 测试 API：`curl http://localhost:8000/api/problem/?limit=10`
3. 查看浏览器控制台的错误信息

### Q: 样式显示异常？
**A**: 
1. 清除浏览器缓存
2. 确认 `.css` 文件已正确加载
3. 尝试切换浅色/深色主题

## 📦 文件清单

新增/修改的文件：
```
youtu-agent/frontend/webui/src/
├── types/problem.ts                    [新增]
├── services/qduojService.ts            [新增]
├── components/ProblemLibrarySidebar.tsx [新增]
├── components/ProblemLibrarySidebar.css [新增]
├── App.tsx                             [修改]
└── index.css                           [修改]
```

## 📚 完整文档

详细功能说明、API 接口、设计细节请参考：
`AI界面题库功能集成完成报告.md`

---

**提示**: 首次使用建议查看完整报告了解所有功能特性！
